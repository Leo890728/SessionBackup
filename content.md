# AI Agent 記憶同步

## 一、需求說明

建立一套 AI Agent 記憶同步機制，將 Claude Code、Codex 等工具產生的使用者設定檔、對話紀錄與 Agent 記憶資料，在不同電腦之間進行同步與還原，讓使用者更換裝置後仍可延續既有的工作脈絡。

針對長期累積而持續增長的對話檔案，導入內容去重、分塊及壓縮機制，降低儲存空間與同步流量；同時建立歷史版本管理流程，保存每次變更、分支與同步結果，支援版本追蹤、衝突處理、指定時間點還原及舊資料封存，確保 AI Agent 記憶能夠安全、有效且持續地跨裝置使用。

- **初始設定**
    - 安裝 AI Session Backup VS Code 擴充功能。
    - 連接 GitHub 帳號，建立或連結專用的私人備份 Repository。
    - 設定每台電腦的 `machineId`，用來區分不同裝置的對話紀錄。
    - 從側邊欄選擇要備份的 AI 對話，可依照工具、Claude 專案或單一對話進行勾選。
    - 可設定自動備份間隔、啟動時同步、單檔大小上限及金鑰掃描功能。
- **對話資料來源**
    - Claude Code 對話紀錄。
    - OpenAI Codex 對話紀錄。
    - 預設從下列本機資料夾讀取：
        - `~/.claude`
        - `~/.codex`
    - Claude Code 依專案分組；Codex 依日期分組。
    - 使用者可選擇備份整個工具、特定 Claude 專案或單一對話。
    - 未勾選的對話不會上傳、同步或觸發自動備份。
- **備份與同步機制**
    - 收集已勾選的對話紀錄。
    - 在提交前掃描可能包含的 API Key、Token、私鑰等敏感資訊，命中時可選擇就地遮蔽後再備份。
    - 遮蔽採用內容雜湊產生的標籤（而非流水號），確保不同電腦各自遮蔽同一段內容時產生相同位元組，避免跨機假衝突；原文只存於本機，可透過指令還原。
    - 將對話內容寫入版本化的 revision 檔案。
    - 建立 Git commit，並推送至 GitHub 私人 Repository。
    - 多台電腦使用相同備份庫時，可同步並合併其他裝置的對話紀錄。
    - 系統會依照對話內容判斷要保留本機版本、採用遠端版本或標記為衝突。
    - 若 Claude 專案在本機解不出對應資料夾，同步不會中斷詢問，而是在側邊欄顯示「待對應」節點，由使用者事後手動指定資料夾，指定後自動再同步一次。
    - 自動備份實際執行的是「先同步遠端，再備份本機」，讓多台電腦之間維持接近即時的同步狀態。
- **備份資料結構**
    - 各台電腦使用獨立的 `machineId` 管理 manifest。
    - 對話 revision 採用內容雜湊儲存，相同內容只保存一次。
    - 備份庫採取 no-delete 原則，已備份的歷史 revision 不會因取消勾選而自動刪除。
    - 即使同一個對話存在於不同本機路徑，也能透過內容雜湊進行去重。

---

## 二、工具、資料來源

### 1. 開發與執行工具

- **Visual Studio Code**
    - 作為擴充功能的執行平台。
    - 提供側邊欄、命令面板、設定頁面及衝突比較介面。
- **VS Code Extension API**
    - 建立 Session Backup 活動列與側邊欄。
    - 顯示 Claude Code、Codex 與各對話節點。
    - 提供勾選、備份、同步、匯出及衝突處理功能。
- **Node.js／npm**
    - 安裝相依套件。
    - 執行測試。
    - 打包產生 `.vsix` 擴充功能安裝檔。

```bash
npm install
npm test
npm run package
code --install-extension session-backup-0.3.0.vsix --force
```

安裝新版擴充功能後需執行 **Reload Window**，開發時也可直接按 `F5` 啟動 VS Code Extension Development Host。

### 2. 備份與版本控制工具

- **Git**
    - 管理備份庫版本。
    - 每次備份建立 commit。
    - 支援本機與遠端紀錄同步。
- **GitHub**
    - 儲存 AI 對話備份。
    - 備份庫必須使用私人 Repository。
    - 若指定的 Repository 為公開狀態，系統會拒絕連接，以避免對話紀錄公開。
    - 預設備份庫名稱為 `agent-session-backup`。

### 3. 對話資料來源

- **Claude Code**
    - 從 `~/.claude` 掃描本機對話紀錄。
    - 依照專案路徑進行分類。
    - 多台電腦同步時，可透過 Git remote hash 或手動指定資料夾進行專案對應。
- **OpenAI Codex**
    - 從 `~/.codex` 掃描 session 與 archived session。
    - 依日期顯示對話。
    - 子代理 thread 與父 thread 視為相同備份單位。

### 4. 備份資料格式

- 原始對話以 JSONL 格式保存。
- revision 儲存路徑：

```
store/<tool>/<session-id>/<sha256>.jsonl
```

- 各裝置的 manifest 儲存於：

```
machines/<machine-id>/manifest.json
```

- 備份庫根目錄包含合法的 `format.json`，用來判斷 Repository 是否為可連接的 AI Session Backup 備份庫。
- Markdown 主要用於對話預覽與匯出，實際備份仍保存原始 JSONL。

### 5. 安全機制

- **金鑰掃描**
    - 預設啟用。
    - 在 commit 前掃描新寫入的對話內容。
    - 支援檢查 Anthropic、OpenAI（含舊版格式）、GitHub、GitLab、AWS、Slack（含 Webhook）、Google、Stripe、npm、PyPI、Hugging Face、Groq、Notion、SendGrid、JWT、連線字串密碼與私鑰標頭等格式。
    - 規則一律採前綴錨定的高精確度樣式，不做熵值啟發式判斷，避免誤判改寫到正常對話內容。
    - 偵測到敏感資訊時，可選擇遮蔽後備份、跳過此次備份、取消選取對話、仍然備份或取消整次作業。
- **金鑰遮蔽（Redaction）**
    - 需使用者明確選擇才會執行，自動背景備份不會主動遮蔽。
    - 就地改寫本機原始 JSONL 檔案，把命中的金鑰換成 `<SECRET:<類別>:<內容雜湊前 8 碼>>` 標籤。
    - 標籤由原文內容雜湊而非流水號產生，確保不同電腦各自遮蔽同一段內容時得到相同結果，避免逐行比對合併時產生永久假衝突。
    - 遮蔽前的原文只保存於本機 VS Code extension 的 globalStorage（`secret-vault.json`），不進備份庫、不進 Settings Sync。
    - 兩分鐘內剛寫入過的檔案會跳過遮蔽，避免與 Agent 仍在寫入的內容衝突。
    - 提供「還原遮蔽的金鑰」指令，可將標籤換回本機原文。
- **私人 Repository 限制**
    - 禁止將對話紀錄連接至公開 GitHub Repository。
    - 降低 AI 對話、程式碼與環境資訊被公開的風險。

---

## 三、測試成果

### 1. 安裝與啟動測試

- 成功透過 npm 安裝專案相依套件。
- 成功執行自動化測試。
- 成功打包產生 `.vsix` 安裝檔。
- 成功於 VS Code 安裝並啟用擴充功能。
- Reload Window 後可載入最新版本程式碼。

### 2. GitHub Repository 連接測試

測試情境包括：

- 找到單一符合條件的私人 Repository 時，自動連接。
- 找到多個候選 Repository 時，顯示清單供使用者選擇。
- 輸入已存在的私人 Repository 名稱時，直接連接。
- 輸入不存在的 Repository 名稱時，自動建立新的私人 Repository。
- 輸入公開 Repository 名稱時，拒絕連接並顯示錯誤。

### 3. 對話選取測試

- 可勾選 Claude Code 或 Codex 工具節點，備份該工具目前及後續新增的全部對話。
- 可勾選單一 Claude 專案，備份該專案全部對話。
- 可只勾選單一對話。
- 可先選取整個專案，再排除其中一筆特定對話。
- 較具體的規則優先於工具或專案層級規則。
- 未選取的對話不會上傳，也不會被其他電腦的版本覆寫。

### 4. 單機備份測試

備份流程：

```
收集已選取的 session
→ 執行金鑰掃描
→ 寫入 revision
→ 建立 commit
→ Push 至 GitHub
```

測試結果：

- 有選取對話時，可正常建立 revision 並推送至 GitHub。
- 沒有選取任何對話時，系統會停止備份並顯示提示。
- 未修改的對話不會重複寫入相同 revision。
- 相同內容可透過 SHA-256 進行去重。

### 5. 多機同步測試

測試流程：

1. 第一台電腦建立並上傳備份。
2. 第二台電腦安裝擴充功能。
3. 第二台電腦設定不同的 `machineId`。
4. 連接相同的 GitHub 備份庫。
5. 執行「同步並合併其他電腦紀錄」。
6. 將遠端對話匯入第二台電腦。

測試結果：

- 遠端存在而本機不存在的對話，可正常匯入。
- 匯入的對話會自動加入已選取狀態。
- 本機尚未選取的既有對話不會被遠端覆寫。
- 不同電腦可透過獨立 manifest 保存裝置狀態。

### 6. 同步判斷測試

| 測試情況 | 系統處理方式 |
| --- | --- |
| 本機與遠端內容相同 | 略過 |
| 遠端是本機內容的完整延伸 | 自動採用遠端 |
| 本機是遠端內容的完整延伸 | 保留本機 |
| 對話檔案最近仍在寫入 | 延後至下次同步 |
| 本機與遠端從同一版本分叉 | 標記為衝突 |
| 本機沒有該對話 | 匯入遠端版本 |
| 本機有對話但未勾選 | 不覆寫、不匯入 |
| Claude 專案在本機解不出對應資料夾 | 不中斷同步，略過該專案對話，於側邊欄顯示「待對應」節點 |

### 7. 側邊欄狀態測試

| 圖示 | 狀態 | 說明 |
| --- | --- | --- |
| ✓ | 已同步 | 目前內容已存在於備份中 |
| ☁ | 未同步 | 備份後又有新增內容 |
| ○ | 待備份 | 已選取但從未備份 |
|   ⃠ | 未選取 | 不參與備份與同步 |
| ⚠ | 跳過（過大） | 超過檔案大小上限 |

側邊欄狀態可直接透過本機檔案的修改時間及大小與 manifest 比對，不需要連接網路。

Claude Code 節點底下另外會顯示「待對應」節點：代表其他電腦備份過、但本機找不到對應資料夾的專案，標示對話數與來源電腦，沒有核取方塊，點擊後可指定本機資料夾並自動再同步。

### 8. 衝突處理測試

- 分叉的對話會顯示於衝突清單。
- 可開啟左右對話比較畫面。
- 使用者可選擇：
    - 保留遠端版本。
    - 保留本機版本。
    - 暫時跳過。
- 保留遠端版本時，本機原始內容仍保存在 store 中。
- 保留本機版本後，只有遠端出現新 revision 時才會再次形成衝突。

### 9. 金鑰遮蔽測試

- 掃描到金鑰時選擇「遮蔽後備份」，可將命中內容就地換成內容雜湊標籤並照常完成備份。
- 相同金鑰在同一份對話中重複出現時，只記錄一次遮蔽紀錄，但所有出現位置都會被取代。
- 不同電腦各自對同一段內容執行遮蔽，因標籤由內容雜湊決定，結果位元組相同，不會產生假衝突。
- 遮蔽前兩分鐘內剛被寫入的檔案會跳過本次遮蔽，留待下次處理。
- 遮蔽後的原文可透過「還原遮蔽的金鑰」指令換回本機檔案。
- 原文僅保存於本機 extension 的 `secret-vault.json`，不會出現在備份庫或推送至 GitHub。

### 10. 待對應專案測試

- 電腦 B 同步電腦 A 備份過、但本機不存在對應資料夾的 Claude 專案時，同步流程不中斷、不跳出視窗。
- 該專案改以「待對應」節點顯示在 Sessions 側邊欄的 Claude Code 底下，標示對話數與來源電腦。
- 點擊待對應節點可選擇「使用目前工作區」或「選擇本機資料夾」完成對應。
- 完成對應後自動觸發一次同步，該專案下的對話正常匯入並落地為一般專案節點。
- 全新電腦即使尚未有任何本機 Claude 對話紀錄，只要備份庫中存在其他電腦的專案，仍會顯示 Claude Code 節點與待對應項目。

### 11. 測試網址

- GitHub Repository：`【待補】`
- 專案原始碼網址：`【待補】`
- VS Code Marketplace 頁面：`【待補，目前若尚未發布可填無】`

### 12. 測試畫面

建議放置下列畫面：

1. Session Backup 側邊欄。
2. GitHub Repository 連接畫面。
3. Claude Code／Codex 對話選取畫面。
4. 已同步、未同步、待備份等狀態畫面。
5. GitHub 備份 Repository 目錄結構。
6. 多台電腦同步結果。
7. 對話分叉衝突比較畫面。
8. 金鑰掃描警告畫面（含遮蔽後備份選項）。
9. Markdown 對話預覽或匯出結果。
10. 擴充功能設定頁面。
11. 側邊欄「待對應」節點與對應資料夾流程。
12. 還原遮蔽金鑰的指令與結果畫面。

---

## 四、討論、後續規劃

- **擴充支援的 AI Agent**
    - 目前主要支援 Claude Code 與 Codex。
    - 後續可加入 Gemini CLI、Cursor、GitHub Copilot CLI 或其他本機 AI Agent。
    - 建議將不同 Agent 的對話來源抽象為統一的 Provider Adapter。
- **改善備份安全性**
    - 目前透過私人 GitHub Repository、擴充的金鑰掃描規則與金鑰遮蔽（就地改寫、內容雜湊標籤、本機保險庫還原）降低資料外洩風險。
    - 後續可增加本機加密後再上傳的功能。
    - 可由使用者自行管理加密金鑰，避免 GitHub 或其他雲端服務直接讀取對話內容。
    - 目前遮蔽只涵蓋高精確度的金鑰格式，尚未涵蓋敏感路徑、電子郵件、IP 位址與個人資料，後續可視需求擴充對應規則。
- **完善還原流程**
    - 增加完整的還原精靈。
    - 可選擇還原單一對話、特定日期或特定電腦的備份。
    - 還原前提供差異預覽，避免覆蓋本機較新的內容。
    - 支援將備份還原至新的專案路徑。
- **改善衝突合併**
    - 目前分叉衝突主要由使用者選擇保留本機或遠端版本。
    - 後續可分析 JSONL 的訊息結構，自動合併沒有互相重疊的訊息。
    - 可提供三方比較，包括共同祖先、本機版本與遠端版本。
    - 可針對不同對話分支提供更清楚的樹狀視覺化。
- **改善對話分支顯示**
    - Claude Code 對話可能因編輯訊息或回退產生分支。
    - 目前 Markdown 預覽是線性顯示，可能將已放棄分支與最終分支混合呈現。
    - 後續可解析 `parentUuid` 等欄位，建立真正的對話樹狀預覽。
- **改善多機設定管理**
    - `machineId` 屬於單機設定，不會透過 Settings Sync 同步。
    - `selectedSessions` 則可能被 Settings Sync 傳到其他電腦。
    - 後續可在首次啟動新裝置時提示使用者，確認是否套用其他裝置的對話選取規則。
    - 可提供更清楚的裝置管理頁面，顯示所有已連接裝置與最後同步時間。
    - Claude 專案對應目前已改為非阻斷式：解不出位置時不再跳視窗詢問，而是在側邊欄顯示「待對應」節點供使用者事後處理；後續可延伸同樣的非阻斷模式到其他需要使用者輸入的情境。
- **支援其他備份平台**
    - 除 GitHub 外，可加入：
        - 自架 Forgejo
        - Gitea
        - GitLab
        - 本機 Git Server
        - WebDAV
        - S3 相容物件儲存
    - 透過儲存後端介面抽象化，避免核心備份流程綁定單一平台。
- **增加備份管理功能**
    - 搜尋歷史對話。
    - 依 Agent、專案、日期、電腦或關鍵字篩選。
    - 顯示各對話 revision 歷史。
    - 比較任意兩個歷史版本。
    - 設定備份保留期限。
    - 清理已不存在裝置的 manifest。
    - 匯出 Markdown、JSON、HTML 或壓縮檔。
- **發布與自動化測試**
    - 建立 GitHub Actions，自動執行測試與打包。
    - 版本建立 Tag 後，自動產生 `.vsix` Release。
    - 後續可發布至 VS Code Marketplace。
    - 增加 Windows、macOS、Linux 的跨平台測試。
    - 增加多機同步、Git 衝突、網路中斷及大型對話檔案的整合測試。